# Build output (rendered HTML site)
public/

# Note: content/ holds reviewable Markdown and IS tracked (review-gate flow).

# Local config with real affiliate IDs (never commit)
config.json

# Logs
run.log

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
